export type FlexProps = {
  row?: boolean;
  grow?: boolean;
  fill?: boolean;
  centered?: boolean;
};

export const Flex: React.FC<FlexProps> = ({ row, grow, fill, centered }) => {
  return <div>hi</div>;
};
