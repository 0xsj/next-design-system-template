import { HomeScreen } from 'app/features/home/screen';
import { Layout } from './Layout';

export const Home = () => {
  return (
    <Layout>
      <HomeScreen />
    </Layout>
  );
};

export default Home;
